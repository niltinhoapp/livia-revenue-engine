{
  "name": "livia-revenue-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Motor interno de prospecção da ConnectWeb para aquisição de clientes da Livia.",
  "scripts": {
    "prospect": "tsx src/cli.ts",
    "build": "tsc --noEmit",
    "test": "tsx --test src/**/*.test.ts"
  },
  "dependencies": {
    "apify-client": "^2.21.0",
    "dotenv": "^17.2.1",
    "openai": "^5.12.0"
  },
  "devDependencies": {
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  }
}
